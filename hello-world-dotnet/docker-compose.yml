version: "3"
services:
  app:
    build:
      dockerfile: "./Dockerfile"
      context: "."
    ports:
      - "80:80"
    environment:
      ASPNETCORE_URLS: "http://+:80"
      ASPNETCORE_ENVIRONMEN: "Development"
